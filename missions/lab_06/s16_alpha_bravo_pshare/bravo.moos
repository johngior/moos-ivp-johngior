//-------------------------------------------------
// NAME: Vehicle Community (Bravo)
// FILE: bravo.moos
//-------------------------------------------------

ServerHost = localhost
ServerPort = 9002
Community  = bravo

// Forest Lake
LatOrigin  = 43.825300 
LongOrigin = -70.330400 

// MIT Sailing Pavilion (use this one)
// LatOrigin  = 42.358456 
// LongOrigin = -71.087589

//-------------------------------------------------
// Antler configuration block
ProcessConfig = ANTLER
{
  MSBetweenLaunches = 200

  Run = MOOSDB          @ NewConsole = false
  Run = uSimMarine      @ NewConsole = false
  Run = pNodeReporter   @ NewConsole = false
  Run = pMarinePID      @ NewConsole = false
  Run = pHelmIvP        @ NewConsole = false
  Run = pShare          @ NewConsole = false
}

//-------------------------------------------------
// pShare configuration block
ProcessConfig = pShare
{
  AppTick    = 4
  CommsTick  = 4

  // Listen for incoming variables from the shoreside on unique port 9202
  input = route = localhost:9202

  // Route vehicle telemetry and visuals to the shoreside (listening on 9200)
  output = src_name=NODE_REPORT_LOCAL, dest_name=NODE_REPORT, route=localhost:9200
  output = src_name=VIEW_POLYGON, route=localhost:9200
  output = src_name=VIEW_SEGLIST, route=localhost:9200
  output = src_name=VIEW_POINT, route=localhost:9200
  output = src_name=APPCAST, route=localhost:9200
}

//-------------------------------------------------
// pHelmIvP configuration block
ProcessConfig = pHelmIvP
{
  AppTick    = 4
  CommsTick  = 4

  // Load the newly shifted behavior file for Bravo
  behaviors  = bravo.bhv
  domain     = course:0:359:360
  domain     = speed:0:4:41
}

//-------------------------------------------------
// pNodeReporter configuration block
ProcessConfig = pNodeReporter
{
  AppTick    = 2
  CommsTick  = 2

  platform_type   = kayak
  platform_length = 4
}

//-------------------------------------------------
// uSimMarine configuration block
ProcessConfig = uSimMarine
{
  AppTick    = 10
  CommsTick  = 10

  // Shift start position 50 meters East so it doesn't spawn on top of Alpha
  START_X       = 50
  START_Y       = 0
  START_SPEED   = 0
  START_HEADING = 180
  PREFIX        = NAV
}

//-------------------------------------------------
// pMarinePID configuration block
ProcessConfig = pMarinePID
{
  AppTick    = 10
  CommsTick  = 10

  DEPTH_CONTROL = false
  ACTIVE_START  = true
  
  // Yaw PID controller
  YAW_PID_KP             = 1.2
  YAW_PID_KD             = 0.0
  YAW_PID_KI             = 0.3
  YAW_PID_INTEGRAL_LIMIT = 0.07

  // Speed PID controller
  SPEED_PID_KP           = 1.0
  SPEED_PID_KD           = 0.0
  SPEED_PID_KI           = 0.0
  SPEED_PID_INTEGRAL_LIMIT = 0.07

  // Maximums
  MAXRUDDER  = 100
  MAXTHRUST  = 100
}